# Architecture

- Django `auction_service/` as main web app.
- `recommendation_service/` as RPyC microservice.
- Scribble protocols in `protocols/`.
- Simulated clients in `clients/`.

(We will add diagrams and more detail later.)
